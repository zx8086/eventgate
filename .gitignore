node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.idea/
.vscode/
coverage/

# Claude Code session/agent state — never commit
.claude/

# AWS deployment state (resource ARNs, IDs)
scripts/deploy/.env.aws

# SQLite outbox local state
data/
*.db
*.db-shm
*.db-wal

# guides/ is a symlink to a centrally-managed stow package
# (shared across multiple projects), not project-local content.
guides
guides/
